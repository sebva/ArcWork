<%= bootstrap_form_for(@course) do |f| %>
    <%= f.text_field :name %>
    <%= f.text_field :start_year %>


    <%= f.collection_select :professor, @professors, :id, :email, {prompt: "Select a category"}, {class: "form-control"} %>

    <%= f.submit  "Apply", class: "btn btn-success" %>
<% end %>